#include <stdio.h>
// #include <stdlib.h>

// ----------MALLOC---------
int main() {
    
    
    printf("Enter the number of elements you want in array: ");

}
    // scanf("%d",&n);

    // int *ptr;   //Use of malloc
    // ptr = (int*) malloc (n*sizeof(int));       //10 size ka array bana

    // // for(int i = 0 ; i<n ; i++){
    // //     printf("Enter number %d : ",i+1);
    // //     scanf("%d",&ptr[i]);                      //User input store kiya
    // // }

    // for (int i = 0; i < n; i++)
    // {
    //     printf("The value at %d of the array is %d \n",i,ptr[i]);
    // }
 

// ----------CALLOC---------

// int main() {
    
    /* The function calloc() is similar to malloc(). It allocates memory for an array with
    all elements initialized to zero. */

    
    // int *ptrC;
    // int noElements;
    // printf("Enter the number of elements you want in the array");
    // scanf("%d",&noElements);

    // ptrC = (int*) calloc (noElements , sizeof(int));

    // for (int i = 0; i < noElements; i++)
    // {
    //    printf("Enter number %d : ",i+1);
    //    scanf("%d",&ptrC[i]);               
    // }

    // for (int i = 0; i < noElements; i++)
    // {
    //    printf("The value at %d of the array is %d \n",i,ptrC[i]);
    // }
 
 

// ----------REALLOC----------
    
//     int n;
//     printf("Enter the number of elements you want in array: ");
//     scanf("%d",&n);

//     int *ptr;   //Use of malloc
//     ptr = (int*) realloc (ptrC , n*sizeof(int));       //10 size ka array bana

//     // for(int i = 0 ; i<n ; i++){
//     //     printf("Enter number %d : ",i+1);
//     //     scanf("%d",&ptr[i]);                      //User input store kiya
//     // }

//     for (int i = 0; i < n; i++)
//     {
//         printf("The new value at %d of the array is %d \n",i,ptr[i]);
//     }
    

//     return 0;
// }


